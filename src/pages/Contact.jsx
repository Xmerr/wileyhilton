import React from 'react';
import { Section } from '@templates';

export const Contact = ({ id }) => (
    <Section
        body={[
            'More to come, but for now: ',
            `My linkedIn is above and I am also reachable via email: wileyhilton@gmail.com`,
        ]}
        id={id}
        title='Contact Me'
    />
);
